# 生产环境配置文件
# https://goframe.org/docs/web/server-config-file-template
server:
  address:     ":8000"
  openapiPath: "/api.json"
  swaggerPath: "/swagger"

# https://goframe.org/docs/core/glog-config
logger:
  level : "info"
  stdout: true

# https://goframe.org/docs/core/gdb-config-file
database:
  default:
    link: "pgsql:postgres:12345678@tcp(postgres:5432)/kg?sslmode=disable"
    debug: false

# Redis配置
redis:
  host: "redis"
  port: 6379
  password: "ma123456"
  db: 0

# 阿里云SMS配置
aliyun:
  sms:
    accessKeyId: "LTAI5tM37fRqPKMuWCSRCWNC"
    accessKeySecret: "bZKXUlQotgul0t0t4cp4Jwuof7MYz0"
    signName: "南京尺牍科技文化"
    templateCode: "SMS_325940806"

# 邮箱配置
email:
  host: "smtp.qq.com"
  port: 587
  username: "2410831777@qq.com"
  password: "wuipiilaiwgeecce"
  from: "2410831777@qq.com"

# 文件上传配置
upload:
  path: "/app/uploads"                        # 生产环境文件上传路径
  maxSize: 52428800                           # 最大文件大小（50MB）
  allowedTypes: # 允许的文件类型
    - "image/jpeg"
    - "image/png"
    - "image/gif"
    - "application/pdf"
    - "text/plain"
    - "application/msword"
    - "application/vnd.openxmlformats-officedocument.wordprocessingml.document" # docx
    - "application/json"
    - "jsonl"
    - "docx"
    - "application/vnd.openxmlformats-officedocument.spreadsheetml.sheet" # xlsx
  # 云存储配置（可选）
  cloudStorage:
    enabled: true                            # 是否启用云存储
    provider: "minio"                         # 云存储提供商：minio, aliyun, aws, qcloud
    endpoint: "minio:9000"                    # MinIO服务端点
    bucket: "uploads"                         # 存储桶名称
    region: "us-east-1"                       # 存储区域
    accessKey: "minioadmin"                   # 访问密钥
    secretKey: "minioadmin"                   # 秘密密钥
    useSSL: false                             # 是否使用SSL
    domain: ""                                # 自定义域名

# 应用配置
app:
  name: "kgplatform-backend"
  version: "1.0.0"
  env: "production"
  domain: "your-domain.com"                   # 生产环境域名
  https: true                                 # 是否启用HTTPS